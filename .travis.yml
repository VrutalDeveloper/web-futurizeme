language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -f Dockerfile.dev -t peaceofsheep/web-futurizeme.test .

script:
  - docker run -e CI=true peaceofsheep/web-futurizeme.test npm run test